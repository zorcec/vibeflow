// Re-export all kanban types from the shared UI package
export type {
  TaskType,
  TaskStatus,
  Priority,
  Task,
  Comment,
  FileEntry,
  Column,
  PanelState,
  TaskModalState,
  FilePreviewState,
  ViewMode,
  AppSettings,
  KanbanApi,
} from '@vibeflow-tools/ui/kanban';
