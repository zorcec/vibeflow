// ── CSS for the Shadow DOM overlay (isolated from host page styles) ───────────
// Imported here so the browser bundle embeds them directly — no separate import.

export { OVERLAY_CSS, HOST_PAGE_CSS } from "../overlay/css.js";
