---
"@vibeflow-tools/cli": patch
---

Mark MCP server as experimental in changelog. MCP exposes 10 task tools via streamable HTTP at `/api/mcp` — manifest-driven with auto-discovery for future commands.
