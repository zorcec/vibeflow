---
"@vibeflow-tools/cli": patch
---

Rename "Remove" to "Unlink" in task details — unlink only removes parent link (never deletes). Delete children only via the Delete dialog (2 modes: roots / recursive).
