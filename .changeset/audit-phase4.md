---
"@vibeflow-tools/cli": patch
---

Comment edits now broadcast to live UI. File uploads reject reserved filenames (.env, .git*, .linked.json). Task schema limits enforced (titles 500 chars, descriptions 10K, URLs 2K).
