---
"@vibeflow-tools/cli": patch
---

Audit Phase 5: push --dry-run + orphan cleanup, MCP tools registered from manifest (single source), verify cancellation via AbortSignal, serve deduplication (createBaseServer), CLI --dry-run flag
