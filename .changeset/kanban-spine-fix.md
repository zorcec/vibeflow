---
"@vibeflow-tools/ui": minor
---

Kanban children tree: fix broken spine lines by moving per-row `::before` to node-level `::before` (cable-line pattern). Spine now renders continuous across nested subtrees without per-row gaps.
