---
"@vibeflow-tools/cli": minor
---

Add "🎨 Prototyping" option to the overlay page-level right-click context menu when `@vibeflow-tools/ui-prototyping` is installed. Clicking the option opens the variant switcher panel via the `__vf_prototyping` API.
